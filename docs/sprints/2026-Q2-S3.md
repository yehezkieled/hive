# Sprint 2026-Q2-S3

**Status:** ✅ Closed 2026-06-04 — goal met (early, day 4 of 14). Frozen.
**Window:** 2026-06-01 → 2026-06-15
**Goal:** Begin Phase 2 Restructure — collapse the worst navigability
blockers in `process/` and retire superseded runtime code, with zero
behaviour change and the test suite green between every step.

## Outcome

✅ **Goal met — closed early on day 4 of 14.** Phase 2 Restructure
advanced on three of its four targets; zero behaviour change, suite
green throughout.

- **006 — `WorkerAgent` → `Worker`** (#38). Naming drift resolved; the
  `CONTEXT.md` flagged-ambiguity line removed.
- **004 — `manager.py` breakup** (#41–#45 → PRs #47–#51). The 2,469-LOC
  god object became a **605-LOC facade** delegating to four tested
  collaborators (`approval_handler`, `wake_scheduler`,
  `message_dispatcher`, `lifecycle_manager`), each with its own
  `tests/process/` suite. [ADR 0006](../adr/0006-god-object-breakup-composition.md)
  records composition-over-mixins as the Phase 2 house pattern.
- **007 ✱ — headless runtime removed** (#52). PTY-only commit:
  `ClaudeSession`, the `use_pty=False` branches, and the `HIVE_USE_PTY`
  flag are gone; the unit suite re-based onto a mocked PTY session.
  [ADR 0007](../adr/0007-pty-only-runtime.md) records the one-way
  PTY-only decision.

Suite: **1146 passing**. Deployed to production (`73095d7`). The full
live maestro-turn smoke (browser + Telegram round-trip) is the one
residual human check.

**Carried forward:** none — all committed tickets shipped. Phase 2's
last target, Vault consolidation (005), was always slated for S4 and
opens there.

## Committed

- [`006-worker-rename`](../tickets/006-worker-rename/) — rename the
  `WorkerAgent` class to `Worker` across `src/` and `tests/`.
  Mechanical, ~half a day, near-zero risk. The Phase 2 warmup; a full
  `plan.md` already exists.
- [`004-manager-py-breakup`](../tickets/004-manager-py-breakup/) —
  split `process/manager.py` (2,469 LOC, one 47-method class) into a
  thin core plus four focused modules. Lands as five PRs, tests green
  between each. The sprint centrepiece.
- [`007-remove-headless-runtime`](../tickets/007-remove-headless-runtime/)
  ✱ — retire the headless / non-PTY runtime path now that PTY is the
  production runtime. Commit to PTY-only: remove `ClaudeSession`, the
  `use_pty=False` branches, the headless session-resume logic, and the
  `HIVE_USE_PTY` flag; re-base the unit suite onto a mocked PTY session.

## Risks

- **Ambitious for one window.** 004 (heavy) and 007 (touches the
  runtime *and* the test harness) are both substantive; only 006 is
  trivial. If the window tightens, 007 slips to a follow-up sprint
  first — 004 is the priority.
- **007 rewrites test-harness assumptions.** The unit suite is pinned
  hermetic with `HIVE_USE_PTY=false` (conftest), i.e. it runs *on* the
  headless path. Removing that path means re-basing the suite onto a
  mocked PTY session — design must solve this before any code is
  deleted.
- **007 is a one-way door.** Dropping the headless fallback commits
  Hive to PTY-only. The advisor's one-shot `claude -p` is a separate
  raw subprocess and stays. The decision warrants an ADR (see the
  ticket).
- **004 concurrency hazards.** The non-reentrant `_state_lock` and the
  fire-and-forget task tracking must survive the split unchanged (see
  004 `research.md`).

## Out of scope

- [`005-vault-consolidation`](../tickets/005-vault-consolidation/) —
  drafted and researched, but deferred to the next sprint to give 004
  room. Phase 2 becomes a ~2-sprint arc.
- Phase 3 (PWA) and Phase 4 (Codex / OpenCode adapters).
- Any behaviour change. All three tickets are pure refactors or
  removals; existing tests pass unmodified (006, 004) or are re-based
  without semantic change (007).

## Definition of done

- `WorkerAgent` returns zero matches in `src/` and `tests/`; the
  `CONTEXT.md` flagged-ambiguity line for it is removed (006).
- `process/manager.py` is thinned to orchestration only, with four new
  sibling modules each carrying isolated tests (004).
- The headless runtime path is gone — no `ClaudeSession`, no
  `use_pty` / `HIVE_USE_PTY` conditionals — and the full suite is green
  on a PTY-mocked harness; an ADR records the PTY-only decision (007).
- `ruff check` + `ruff format --check` + full
  `pytest -m "not integration"` green; a maestro turn completes
  end-to-end on the refactored code (smoke test, not suite-only).
- INDEX rows for 006 / 004 / 007 flipped to `done`.
