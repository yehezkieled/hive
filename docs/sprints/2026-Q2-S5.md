# Sprint 2026-Q2-S5

**Status:** Active
**Window:** 2026-06-09 → 2026-06-23
**Goal:** Make Team Leads orchestrate leaf work through the Claude Code
**Workflow** tool instead of spawning persistent Workers — deterministic,
cheap, reliable fan-out — and surface a running Workflow's progress on the
dashboard and Telegram so the org stays visible from your phone. Begin
retiring the persistent Worker entity (the leaf-coordination layer that
carries most of Hive's reliability tax). Plus two carry-forward cleanups:
retire the custom advisor and trim the VPS skill library.

## Committed

- [`013-retire-custom-advisor`](../tickets/013-retire-custom-advisor/) ✱ —
  retire Hive's bespoke `claude -p` advisor MCP tool for Claude Code's
  native `/advisor`. Carry-forward.
- [`014-trim-vps-skill-library`](../tickets/014-trim-vps-skill-library/) —
  filesystem-level removal of unused installed CC skills; the global
  complement to 012's per-role denylist. Follow-up to 012.
- [`015-lead-workflow-leaf-engine`](../tickets/015-lead-workflow-leaf-engine/)
  — give Leads the Workflow tool for leaf fan-out; relax the lead
  `Agent`/`Task` guard. The foundation. ADR-worthy.
- [`016-migrate-leaf-dispatch-to-workflow`](../tickets/016-migrate-leaf-dispatch-to-workflow/) ✱
  — reroute leaf execution from `spawn_worker` to the Workflow engine; drains
  the path that must be empty before the Worker type can be deleted.
- [`017-bridge-workflow-progress`](../tickets/017-bridge-workflow-progress/) ✱
  — bridge a running Workflow's progress (count / phase / partial results /
  completion) into the dashboard + Telegram, reusing Ticket 003's pattern.
  **Absorbs 027** (the 016-smoke false-timeout fix) as slice #118 — the
  reader's liveness-reset + friendly timeout message; 027 is superseded, not
  worked separately.
- [`018-retire-worker-entity`](../tickets/018-retire-worker-entity/) ✅ —
  delete the persistent Worker entity once nothing routes through
  `spawn_worker`. **Done** (#132–#137, PR #139): the `Worker` type and all
  its machinery are deleted; suite green (1166 passed); merged + deployed
  (service boots clean, `purge_role` guard live, `/api/org` worker-free).
  The shared 016/018 maestro→lead→leaf live smoke is the remaining sprint-DoD
  item, run together with closing 016.
- [`023-activate-worktree-floor`](../tickets/023-activate-worktree-floor/) —
  wire `WorktreeManager` into production (`__main__.py` never constructs it,
  so 015's floor is inert and leads run in the live checkout) + fix
  lead→maestro peer delivery + hardcoded-defaults audit. **Pulled in
  2026-06-11**: blocks 015's live DoD, and 016 must not migrate more leaf
  work onto Workflow while leaf agents can edit the live `src/`.
- [`026-turn-boundary-acceptance`](../tickets/026-turn-boundary-acceptance/) —
  reader accepts turns mid-flight during post-tool thinking gaps, silently
  losing `hive_actions` (found by 023's live smoke). **Pulled in
  2026-06-11**: gates the same "end-to-end on deployed code" DoD line and
  fires on every 016-style long Workflow turn.
- [`028-scheduler-poke-gate-guard`](../tickets/028-scheduler-poke-gate-guard/) —
  the scheduler facts-poke injects into a PTY parked at an interactive gate
  and submits the highlighted default, so a maestro "answers" a gate the user
  never saw. **Pulled in 2026-06-13**: the most dangerous 016-smoke finding
  (an unauthorised maestro decision); the fix is a cheap pending-gate guard.
  (027, the other 016-smoke fix, rides 017 as #118; 029 stays S6.)

## Execution order

Blocker graph (what must land before what):

```
  013 retire advisor  ──(independent)──┐
  014 trim VPS skills ──(independent)──┤
                                       │
  015 Lead leaf engine ──▶ 023 activate floor ──┬──▶ 016 migrate dispatch ──▶ 018 retire Worker
      (✅ done)                                 └──▶ 017 progress bridge (✅ done)

  016 live smoke (2026-06-13) ─┬─▶ 027 false timeout   → absorbed into 017 (#118)
                               ├─▶ 028 gate-poke guard → pulled into S5
                               └─▶ 029 gate bridge     → stays S6
```

Suggested order:

1. **015 first** — the foundation; 016/017/018 all block on it. ADR-worthy
   (reverses the lead `Agent`/`Task` guard) → run the full design grill.
   ✅ Done (#74–#79).
2. **013 / 014** — independent carry-forwards; start any time, in parallel.
3. **023** — pulled in 2026-06-11 after the 015 live smoke proved the floor
   inert in production; gates 016 (don't widen the live-checkout hazard) and
   the sprint DoD ("end-to-end on deployed code").
4. **016 + 017** — once 023 lands (017 can trail 016). 017 also carries the
   absorbed 027 (false-timeout) fix as #118.
   ✅ **017 done** (#116–#119) — adapter surface, sweeper, reader liveness-reset
   (+027), dashboard run-card; merged and deployed (WorkflowWatcher live).
5. **028** — pulled in 2026-06-13 from 016's live smoke (the dangerous one: a
   scheduler poke phantom-answering a parked gate). Cheap pending-gate guard,
   independent — land any time.
6. **018 last** — after 016 drains `spawn_worker`. ✅ **Done** (#132–#137,
   PR #139) — built test-first in one session, merged + deployed.

One ticket = one worktree = one session (Phase B). Don't start 016/017/018
before 015's design lands.

## Risks

- **Big slice for the window.** The weight is in 015/016/018; 013/014 are
  small. If it overruns, **018 (the deletion) is the natural spill into S6** —
  (c) is the target end-state, reached when 018 lands.
- **016 + 018 collide with `dispatch.py`** — *corrected 2026-06-11*: the
  autonomous leaf path actually lives in `message_dispatcher.py` +
  `lifecycle_manager.py`; `dispatch.py` holds only the user-facing
  `/worker spawn` command. 016 is smaller than feared; the god-object
  cleanup stays parked unless 018 forces it.
- **015 reverses a deliberate guard** — leads currently block `Agent`/`Task`
  so they spawn Hive Workers, not CC subagents (`lifecycle_manager.py:69-76`).
  Relaxing it is an ADR-level decision; weigh against role-discipline drift.
- **Nested worktrees** — a Lead runs in a Hive worktree; Workflow agents with
  `isolation: 'worktree'` create worktrees under it. Git allows it, but this
  session hit worktree quirks (`'main' already used by worktree`) — the
  integration must handle them, not assume them away.
- **Visibility regression** — ephemeral Workflow agents don't appear in the
  org tree the way Workers do; 017 must land or the dashboard goes blind to
  leaf work.

## Out of scope

- The named-workflow **interaction-pattern library** (debate, blackboard,
  tournament, research-consolidate …) and the skills that route Leads to them
  — **Track 2**, S6+. S5 ships the engine, not the pattern catalogue.
- **Steering** Workflow agents from the phone (write-back control) — S6+.
  S5's bridge is read-only visibility first.
- Running leaf agents on **non-Claude models** (the cheap-model idea) — needs
  the Phase 4 adapter layer; not this sprint.
- Phase 3 (PWA) — shifted (see roadmap).
- **029** (maestro `AskUserQuestion` gate not bridged) — the third 016-smoke
  finding. A pre-existing Ticket 003 bridge gap, not 016's own bug → stays
  **S6**. Of the three smoke findings, 027 rides 017 (#118) and 028 is pulled
  into S5; only 029 defers.

## Definition of done

- A Team Lead can decompose a scoped goal and execute it as a **Workflow** run
  (deterministic fan-out), with no `spawn_worker` on the leaf path.
- A running Workflow's progress is visible on the dashboard and pings Telegram
  (count / phase / completion).
- `spawn_worker` is drained from leaf dispatch; the Worker entity is deleted
  (or 018 is explicitly carried to S6 with the path already drained).
- 013: Hive no longer spawns `claude -p`; native `/advisor` enabled.
- 014: the agreed unused skills are removed from the VPS.
- `ruff` + full `pytest -m "not integration"` green; a maestro turn completes
  end-to-end on deployed code.
- 023: the live service constructs `WorktreeManager`; a deployed
  maestro→lead→leaf turn leaves the main checkout clean.
- 028: the scheduler skips/defers an entity parked at an interactive gate; no
  prompt injection can submit a gate's default option.
- INDEX rows for 013–018 + 023 + 028 reflect final state.
