# Sprint 2026-Q2-S2

**Status:** Active
**Window:** 2026-05-29 → 2026-06-12
**Goal:** Harden the PTY runtime so it's robust before the 2026-06-15
headless-billing cutoff — handle interactive gates (plan mode,
`AskUserQuestion`) instead of hanging.

## Committed

- [`003-plan-interaction-bridge`](../tickets/003-plan-interaction-bridge/)
  — detect interactive gates from the transcript, surface them to the
  user, and route the decision back into the PTY. Design brainstorm in
  progress (paused at the core interaction-model question).

## Risks

- **Phase 1 deadline 2026-06-15** — 3 days before sprint close. The
  PTY runtime is already live and plan-billed (Phase 1 goal met); 003
  is robustness, not a blocker. But interactive-gate hangs degrade
  real use, so land 003 before relying on plan mode in production.
- **003 design not finalized.** Brainstorm paused at Q1; needs the
  user's answer on the interaction model (notify-only vs. approve
  round-trip) and any cortexOS prior art before implementation.

## Out of scope

- Phase 2 — `manager.py` breakup, Vault consolidation, `WorkerAgent`
  → `Worker` rename
- CHANGELOG/`DEPLOYMENT.md` sprint-stamp cleanup
- Permission-prompt gating beyond what `yolo`/`bypassPermissions`
  already cover (confirm during 003 design)

## Definition of done

- A maestro in `plan` mode completes a Telegram turn without hanging:
  the plan reaches the user and the turn proceeds on approval or parks
  cleanly.
- An `AskUserQuestion` mid-turn no longer hangs to the 180s timeout.
- No screen-scraping introduced; gate detection reads the transcript.
- Ticket 003 marked done in `docs/tickets/INDEX.md`.
