# Sprint 2026-Q2-S1

**Window:** 2026-05-26 → 2026-06-09
**Goal:** Deploy Sprint 30 (PTY) + Sprint 31 (QuotaMonitor) to
production. Land the new project-management doc structure.

## Committed

- [`001-deploy-pty-runtime`](../tickets/001-deploy-pty-runtime/) —
  push, restart, verify, flip `HIVE_USE_PTY=true`.
- [`002-doc-restructure`](../tickets/002-doc-restructure/) — adopt
  three-altitudes structure (this ticket itself).

## Risks

- **Phase 1 deadline 2026-06-15** — 6 days after sprint close. If
  PTY deploy regresses or rolls back, need a fallback path before
  headless billing flips to API.
- **Doc work could eat deploy budget.** Mitigation: ticket 001 is
  half a day; ticket 002's research/design is already produced by the
  grilling conversation that scoped it.

## Out of scope

- Phase 2 — `manager.py` breakup
- `DEPLOYMENT.md` sprint-stamp cleanup (separate Ticket)
- `ARCHITECTURE.md` creation (not minimum viable; deferred)
- Workflow enforcement (blocked on multi-CC sandboxing)

## Definition of done

- `hive.service` live with `HIVE_USE_PTY=true`; `/quota` returns data
- New doc structure populated (`docs/sprints/`, `docs/tickets/`,
  `docs/archive/`); legacy docs archived
- `CLAUDE.md` and `README.md` refreshed
- Both Tickets marked done in `docs/tickets/INDEX.md`
- CHANGELOG entry added for the Sprint 30 + 31 deployment
